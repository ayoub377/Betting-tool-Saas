import { MarketValuePipe } from './market-value.pipe';

describe('MarketValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MarketValuePipe();
    expect(pipe).toBeTruthy();
  });
});
